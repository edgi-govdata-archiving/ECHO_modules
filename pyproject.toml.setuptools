[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ECHOmodules"
version = "1.0.1"

[options]
package_dir = "src"

[options.packages.find]
where = "src"
